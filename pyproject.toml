[tool.poetry]
name = "clickhouse-db-copy"
version = "0.1.0"
description = ""
authors = ["Daniel_Yuan_Ren"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
